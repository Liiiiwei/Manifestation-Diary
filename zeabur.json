{
    "$schema": "https://zeabur.com/schema.json",
    "build": {
        "buildCommand": "npm install && npm run build"
    },
    "deploy": {
        "startCommand": "caddy run --config Caddyfile & node server.js"
    },
    "env": {
        "NODE_ENV": "production"
    }
}